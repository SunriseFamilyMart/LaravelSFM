name: Deploy to Server

on:
  push:
    branches:
      - Live

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SERVER_HOST }}      # STATIC IPv4 ONLY
          username: ${{ secrets.SERVER_USER }}  # usually: ubuntu
          key: ${{ secrets.SERVER_SSH_KEY }}    # PRIVATE SSH KEY (multiline)
          port: 22
          ssh_options: -4                       # 🔥 FORCE IPv4 (FIXES TIMEOUT)
          script: |
            set -euo pipefail

            echo "🚀 Deploy started at $(date)"

            APP_DIR="/var/www/html/golden_brown"
            BRANCH="Live"

            cd "$APP_DIR"

            echo "📥 Fetching latest code..."
            git fetch origin

            echo "🔀 Switching to $BRANCH branch..."
            git checkout "$BRANCH"

            echo "⬇ Pulling latest changes..."
            git pull origin "$BRANCH"

            echo "🧹 Clearing Laravel caches..."
            php artisan optimize:clear

            echo "🔐 Fixing permissions..."
            sudo chown -R www-data:www-data storage bootstrap/cache
            sudo chmod -R 775 storage bootstrap/cache

            echo "♻ Reloading PHP-FPM..."
            sudo systemctl restart php8.3-fpm

            echo "✅ Deploy finished successfully at $(date)"
